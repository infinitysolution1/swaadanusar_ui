import React from "react";

export const metadata = {
  title: "Why Liquid Salt | Swaad Anusar",
  description: "India's first Alkaline liquid salt",
};

const Layout = ({ children }) => {
  return <main>{children}</main>;
};

export default Layout;
